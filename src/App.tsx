import { Button } from "@chakra-ui/react";
import "./App.css";

function App() {
  return (
    <div>
      <Button colorScheme="teal" size="lg">
        Button
      </Button>
    </div>
  );
}

export default App;
